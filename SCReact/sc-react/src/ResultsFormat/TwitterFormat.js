import { render } from '@testing-library/react';
import React, {Component} from 'react';
import {Table} from 'react-bootstrap';
import "./Post.css";

export function TwitterFormat(props){
    return(
        <div>
            Add Twitter Formatting
        </div>
        )
    }
    
export default TwitterFormat;